# Core dependencies for the credit-risk pipeline.
# Versions are lower-bounded for reproducibility; the pipeline is tested
# against the 1.x scikit-learn API.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
matplotlib>=3.7
joblib>=1.3

# --- Optional "premium stack" drop-ins (see src/credit_risk/models.py) ------
# Uncomment to swap the sklearn gradient-booster / MLP for library versions.
# xgboost>=2.0
# lightgbm>=4.0
# skorch>=0.15      # PyTorch MLP wrapped as an sklearn classifier
# torch>=2.1

# --- Development -------------------------------------------------------------
# pytest>=7.4
